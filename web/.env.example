# Base URL of the Krishi Sech Express API, with no trailing slash.
# The admin panel calls ${NEXT_PUBLIC_API_BASE_URL}/api/admin/* from the browser,
# so this origin must also be listed in the backend's CORS_ALLOWED_ORIGINS.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
